{
  "name": "hermes-office",
  "private": true,
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/tonbistudio/hermes-office.git"
  },
  "scripts": {
    "build": "npm run build --prefix word && npm run build --prefix excel",
    "test": "node --test",
    "lint": "npm run lint --prefix word && npm run lint --prefix excel"
  }
}
