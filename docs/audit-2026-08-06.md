# Audit — 2026-08-06

Phạm vi: toàn repo (`word/`, `excel/`, `shared/`, `scripts/`, build/manifest, docs/ADR). Phương pháp: `npm test` + `npm run lint` chạy thật, cộng 3 lượt review độc lập (bảo mật, bug chức năng, chất lượng code), mỗi phát hiện đối chiếu file:line với source thật. Không có finding nào tự ý sửa — audit only.

## Baseline đo được

- `npm test`: **103/103 pass**, 0 fail.
- `npm run lint`: **0 error**, 12 warning — toàn bộ là `office-addins/no-context-sync-in-loop` (word: proposal-mgr.js, selection-mgr.js; excel: taskpane.js) và `no-navigational-load` (word: proposal-mgr.js, selection-mgr.js). Đây là warning hiệu năng Office.js, không phải bug đúng nghĩa — batching `context.sync()` ngoài vòng lặp sẽ giảm round-trip tới Word/Excel.

## Bảo mật

| # | Mức | Phát hiện | Bằng chứng |
|---|-----|-----------|------------|
| SEC-01 | Medium | `literalCellValue` (`excel/src/taskpane/actions.js:16-17`) chỉ test ký tự đầu (`/^[=+\-@]/`). Giá trị có khoảng trắng/tab/newline dẫn đầu (`" =WEBSERVICE(...)"`) không bị chặn, ghi thẳng qua `Range.values` (`taskpane.js:446,451`). `actions.test.js:16-29` chỉ test prefix đúng ngay ký tự đầu — chưa test case whitespace-leading. Rủi ro tăng khi file rời khỏi Excel Desktop (Sheets/LibreOffice/CSV thường trim whitespace trước khi nhận diện công thức). | `excel/src/taskpane/actions.js:16-17` |
| SEC-02 | Medium | Dev server (`word/webpack.config.js:97-108`, `excel/webpack.config.js:99-113`) set `Access-Control-Allow-Origin: *` **và** proxy `/v1` thẳng tới Local Gateway (khóa API đính kèm) — đúng chính xác kịch bản mà comment bảo mật trong `Caddyfile.example:16-18` nói là phải tránh ("A wildcard would let any page in any browser on this machine drive your Provider"). Khi `npm run dev-server`/`webpack serve --mode development` đang chạy, bất kỳ trang nào khác trên máy có thể POST tới `:3000/v1/chat/completions` và tiêu quota. Không có ADR/README nào nhắc tới rủi ro dev-only này. | `word/webpack.config.js:97-108`, `excel/webpack.config.js:99-113` |
| SEC-03 | Low | Regex rewrite manifest production (`urlDevPattern = /https:\/\/localhost:3000\//g`) yêu cầu dấu `/` sau `:3000`. `<AppDomain>https://localhost:3000</AppDomain>` (không có path) không khớp regex → **sống sót vào bản production** `dist/manifest.xml`, mở rộng AppDomain trust ngoài ý muốn. Verify trực tiếp: `word/manifest.xml:17` đúng là `https://localhost:3000` không path. | `word/webpack.config.js:10`, `word/manifest.xml:16-19` (tương tự excel) |
| SEC-04 | Low | `scripts/setup.mjs:24-27` dùng `readline.question()` trần cho prompt "Khóa API:" — không mask input. Key hiện rõ trên terminal (screen-share, session recording, shoulder-surfing). | `scripts/setup.mjs:24-27,161` |
| SEC-05 | Low | Staleness check của Excel (`taskpane.js:361-368`) chỉ re-verify sheet trùng `proposalSheetName`. Model được phép đề xuất action tham chiếu sheet khác (`Sheet!A1`, `resolveRange` hỗ trợ ở `shared/parsers.js:145-151`) — sheet đó không được re-check trước khi ghi, mâu thuẫn với tuyên bố README "Excel từ chối áp dụng thay vì ghi mù" nếu sheet đổi. | `excel/src/taskpane/taskpane.js:361-368` |

Không báo cáo lại các rủi ro đã được ADR-0001/0002/0003 chấp nhận có chủ đích (Custom Function bypass khi bật, không auth header client-side, single-key rủi ro toàn máy).

## Bug chức năng

| # | Mức | Phát hiện | Test coverage |
|---|-----|-----------|----------------|
| BUG-01 | High | `findProposalTable` (`word/src/taskpane/proposal-mgr.js:72-73`) dedup bằng `new Set(sel.tables.items.concat(body.tables.items))`. Office.js `.items` trả proxy object **mới mỗi lần đọc** → cùng 1 bảng thật xuất hiện ở cả `sel.tables` và `body.tables` là 2 reference khác nhau, `Set` không dedup được → bảng khớp bị đếm 2 lần → `matches.length === 1` fail sai → Apply hợp lệ bị từ chối bất cứ khi nào bảng target đang được chọn. **Không có test** cho path thành công (chỉ test path 0-match). Verified trực tiếp trên source. | Không |
| BUG-02 | Medium | `fetchWithTimeout` (`shared/hermes.js`) không abort request đầu khi retry sau timeout (`Promise.race`, không `AbortController` — chủ ý vì WKWebView). Request đầu vẫn chạy nền → 1 lần Ask có thể sinh 2 lần completion đầy đủ, tốn gấp đôi chi phí/quota, kết quả đầu bị âm thầm bỏ. | Partial — test đếm số lần gọi, không test double-cost |
| BUG-03 | Medium | `Idempotency-Key` chỉ được gửi bởi Excel Custom Functions; 2 luồng chat chính (Word `askHermes(payload)`, Excel `askHermes(history)`) không gửi key này — nên bug BUG-02 hoàn toàn không có cơ chế dedup cho chat thường. | Không |
| BUG-04 | High (UX+leak) | `word/src/taskpane/taskpane.js:335-337`: mọi lỗi Ask hiện `err.stack` (đường dẫn file, số dòng nội bộ) thẳng vào bong bóng chat cho user thấy. Excel tương ứng chỉ hiện `e.message`. Đã verify trực tiếp trên source. | Không (taskpane.js UI wiring không có test file) |
| BUG-05 | Medium | `applyText` (`proposal-mgr.js:157-207`) — chính là logic Pin/bookmark staleness check mà CONTEXT.md mô tả — **0% test coverage**. 3 nhánh (bookmark khớp / bookmark cũ fallback unique-search / text quá dài fail cứng) đều chưa test. | Không |
| BUG-06 | Low | `getSnapshot()` Excel (`taskpane.js:246-255`) truncate selection lớn (rows/cols/bytes) **âm thầm**, không set truncation flag như used-range snapshot bên cạnh nó → model trả lời dựa trên dữ liệu bị cắt mà user không biết. | Không |
| BUG-07 | Low | Word `applyFulldocEdits` gộp mọi lỗi khác nhau thành 1 thông báo "không tìm thấy hoặc quá dài" kể cả khi nguyên nhân thật khác (WordApi permission/range error) — Excel report lỗi per-action chính xác hơn. | Partial |

## Chất lượng code / cải tiến tiềm năng

| # | Ưu tiên | Phát hiện |
|---|---------|-----------|
| Q-01 | High | Word thiếu cap kiểu `MAX_ACTIONS=100` (excel/src/taskpane/actions.js:8) cho `parseWordEdits`/`parseWordTableChanges` (`word/src/taskpane/taskpane.js:301-326`) — model reply runaway có thể sinh proposal hàng trăm/nghìn edit, phá vỡ chính nguyên tắc "Apply chỉ là ranh giới bảo mật khi user đọc được thẻ" (CONTEXT.md). Excel đã có guard này, Word thì chưa — bất đối xứng đáng chú ý. |
| Q-02 | Medium | Hai switch-statement độc lập format Action thành text: `describe()` (`excel/src/taskpane/actions.js:52-69`) và `describeAction()` (`shared/proposal-card.js:166-233`) — đã lệch nhau (describeAction xử lý cả replace/insert/delete, describe() thì không). Rủi ro copy-paste giữa 2 nơi mà `shared/` vốn được tạo ra để tránh. |
| Q-03 | Low | `word/package.json` vs `excel/package.json`: version webpack/webpack-dev-server/@babel/preset-env lệch nhau không rõ lý do; excel có `prettier` script/config, word thì không dù cùng extend 1 ESLint config. |
| Q-04 | Low | `excel/package.json` khai `os-browserify`, `process`, `source-map-loader` nhưng không có chỗ nào trong `excel/webpack.config.js` dùng tới (grep 0 hit) — leftover từ setup cũ, có thể gỡ sau khi xác nhận build vẫn qua. |
| Q-05 | Low | `MAX_HISTORY_MESSAGES=20` và logic trim history được viết tay 2 lần độc lập (Word slice, Excel splice) — cùng policy, 2 implementation có thể trôi dần theo thời gian. |
| Q-06 | Low | Context bar Word dùng `shared/proposal-card.js` (`mountContextBar`), Excel tự viết `refreshMeta()`/`setMetaItem()` riêng — cơ hội hợp nhất UI mà `shared/` vốn tồn tại để phục vụ, chưa được tận dụng cho feature này. |
| Q-07 | Trivial | `parseWordEdits`/`parseWordTableChanges` trong `shared/proposal-card.js:380-386` chỉ là wrapper 1 dòng pass-through quanh `shared/parsers.js` — hop thừa, có thể import thẳng từ parsers.js. |

## Không phải finding (đã loại)

- `install.sh`/`install.ps1` không có test riêng: chấp nhận được — chỉ orchestrate logic đã test kỹ (`setup.mjs`, `launchagent.mjs`).
- `applyFulldocEdits` không re-verify match count *tại thời điểm* Apply (chỉ verify count lúc hiển thị card): README chỉ cam kết "count hiện trước khi bấm", không cam kết re-verify tại click — không mâu thuẫn tài liệu, chỉ là hành vi biên đáng lưu ý cho maintainer.
