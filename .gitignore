# dependencies / build (any app subfolder)
**/node_modules/
**/dist/

# secrets — NEVER commit these
Caddyfile
.env
*.pem
*.key
**/certs/

# misc
*.log
.DS_Store

# Hermes agent scratch — keep design prototypes, ignore runtime cache
.hermes/cache/
.hermes/*.log
